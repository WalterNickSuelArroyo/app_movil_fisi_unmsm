import 'package:flutter/material.dart';

class AsignacionAulaPage extends StatelessWidget {
  const AsignacionAulaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'asignacion'
        ),
      ),
    );
  }
}